name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
          cache: pip
      - run: pip install -e ".[dev]"
      - run: ruff check .

  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.11", "3.12", "3.13"]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - run: pip install -e ".[dev,gateway]"
      # Model-marked tests need GLiNER2 weights on disk. A stock runner has
      # none, and downloading 2 GB of them on every push to prove the same
      # thing a local run proves is not a trade worth making.
      - name: Test
        shell: bash
        run: |
          set -o pipefail
          pytest --cov=privaparse --cov-report=term-missing | tee pytest-output.log
      - name: Coverage summary
        if: always()
        shell: bash
        run: tail -3 pytest-output.log >> "$GITHUB_STEP_SUMMARY"
