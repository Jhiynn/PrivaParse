# Generated from pyproject.toml — the canonical source is [project.dependencies] there.
# Install for development with:  pip install -e ".[dev]"
# Add the model backend with:   pip install -e ".[model,dev]"

typer>=0.12
pydantic>=2.0
pydantic-settings>=2.0
sqlalchemy>=2.0
alembic>=1.13
phonenumbers>=8.13

# --- extras: model (Milestone D) ---
# gliner2[local]>=1.3.2

# --- extras: dev ---
# pytest>=8.0
# pytest-cov>=5.0
